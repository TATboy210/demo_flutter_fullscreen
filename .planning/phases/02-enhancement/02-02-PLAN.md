---
phase: 02-enhancement
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - demo_fullscreen/lib/main.dart
autonomous: true
requirements:
  - API-01
  - API-02
  - API-03
  - API-04
  - DEP-01
  - DEP-02
  - DEP-03
must_haves:
  truths:
    - "页面包含可折叠的 API 对比区域，展示方法签名、返回值、平台支持、架构差异"
    - "页面包含依赖信息区域，展示依赖树、体积、已知陷阱"
    - "所有信息面板使用 ExpansionTile 实现折叠/展开"
    - "平台支持矩阵使用 DataTable 展示，支持的平台显示绿色勾，不支持的显示红色叉"
  artifacts:
    - "demo_fullscreen/lib/main.dart (包含 API 对比面板、依赖信息面板、陷阱面板)"
  key_links:
    - "API 签名数据来自 research/FEATURES.md 的 API Surface 部分"
    - "平台支持矩阵来自 research/FEATURES.md 的 Platform Support 表"
    - "架构差异来自 research/ARCHITECTURE.md 的 Key Architectural Differences"
    - "依赖树来自 research/STACK.md 的 Dependencies 部分"
    - "已知陷阱来自 research/PITFALLS.md 的 Pitfall Matrix"
---

<objective>
为双包全屏对比页面添加 API 签名对比面板、依赖信息面板和已知陷阱面板，展示两个包的完整技术对比信息。

Purpose: 提供全面的技术参考信息，让用户无需查阅外部文档即可了解两个包的 API 差异、依赖关系和潜在问题。
Output: main.dart 扩展为包含三个可折叠信息面板（API 对比、依赖信息、已知陷阱），使用 DataTable 和结构化布局展示对比数据。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/research/FEATURES.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@demo_fullscreen/lib/main.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 添加 API 签名对比面板（方法签名、返回值、平台支持、架构差异）</name>
  <files>demo_fullscreen/lib/main.dart</files>
  <behavior>
    - 页面包含可折叠的"API 对比"ExpansionTile 面板
    - 面板内包含四个子区域：方法签名对比表、返回值差异表、平台支持矩阵、架构差异表
    - 方法签名对比表使用 DataTable，列出两个包的公开方法签名
    - 返回值差异表对比 Future<void> vs void、有无状态回读等
    - 平台支持矩阵使用 DataTable，支持的平台显示绿色勾图标，不支持的显示红色叉图标
    - 架构差异表对比插件类型、原生代码、平台分发、状态模型等维度
    - 所有表格数据硬编码（来自 research 数据）
  </behavior>
  <action>
    **1. 添加 API 对比面板构建方法 (per API-01, API-02, API-03, API-04):**

    在 _DualFullscreenPageState 中添加 `_buildApiComparisonPanel()` 方法，返回一个 Card 包裹的 ExpansionTile：

    ```dart
    Widget _buildApiComparisonPanel() {
      return Card(
        margin: const EdgeInsets.symmetric(horizontal: 16, vertical: 8),
        child: ExpansionTile(
          title: const Text('API 对比', style: TextStyle(fontWeight: FontWeight.bold)),
          initiallyExpanded: false,
          children: [
            // 方法签名对比
            _buildSectionTitle('方法签名对比'),
            _buildSignatureTable(),
            // 返回值差异
            _buildSectionTitle('返回值差异'),
            _buildReturnValueTable(),
            // 平台支持矩阵
            _buildSectionTitle('平台支持矩阵'),
            _buildPlatformMatrix(),
            // 架构差异
            _buildSectionTitle('架构差异'),
            _buildArchitectureTable(),
            const SizedBox(height: 16),
          ],
        ),
      );
    }
    ```

    **2. 构建方法签名对比表 (per API-01):**

    ```dart
    Widget _buildSignatureTable() {
      return Padding(
        padding: const EdgeInsets.symmetric(horizontal: 16),
        child: DataTable(
          columns: const [
            DataColumn(label: Text('维度')),
            DataColumn(label: Text('fullscreen_window')),
            DataColumn(label: Text('flutter_fullscreen')),
          ],
          rows: const [
            DataRow(cells: [
              DataCell(Text('设置全屏')),
              DataCell(Text('Future<void> setFullScreen(bool)', style: TextStyle(fontSize: 12))),
              DataCell(Text('void setFullScreen(bool)', style: TextStyle(fontSize: 12))),
            ]),
            DataRow(cells: [
              DataCell(Text('查询状态')),
              DataCell(Text('无', style: TextStyle(color: Colors.red))),
              DataCell(Text('bool isFullScreen', style: TextStyle(fontSize: 12))),
            ]),
            DataRow(cells: [
              DataCell(Text('监听变化')),
              DataCell(Text('无', style: TextStyle(color: Colors.red))),
              DataCell(Text('FullScreenListener (4 回调)', style: TextStyle(fontSize: 12))),
            ]),
            DataRow(cells: [
              DataCell(Text('屏幕尺寸')),
              DataCell(Text('Future<Size> getScreenSize(BuildContext?)', style: TextStyle(fontSize: 12))),
              DataCell(Text('无', style: TextStyle(color: Colors.red))),
            ]),
            DataRow(cells: [
              DataCell(Text('初始化')),
              DataCell(Text('无需初始化', style: TextStyle(color: Colors.green))),
              DataCell(Text('await FullScreen.ensureInitialized()', style: TextStyle(fontSize: 12))),
            ]),
          ],
        ),
      );
    }
    ```

    **3. 构建返回值差异表 (per API-02):**

    ```dart
    Widget _buildReturnValueTable() {
      return Padding(
        padding: const EdgeInsets.symmetric(horizontal: 16),
        child: DataTable(
          columns: const [
            DataColumn(label: Text('维度')),
            DataColumn(label: Text('fullscreen_window')),
            DataColumn(label: Text('flutter_fullscreen')),
          ],
          rows: const [
            DataRow(cells: [
              DataCell(Text('setFullScreen 返回值')),
              DataCell(Text('Future<void>（可 await）')),
              DataCell(Text('void（fire-and-forget）')),
            ]),
            DataRow(cells: [
              DataCell(Text('状态确认方式')),
              DataCell(Text('无（需手动跟踪）')),
              DataCell(Text('listener 回调确认')),
            ]),
            DataRow(cells: [
              DataCell(Text('错误处理')),
              DataCell(Text('try-catch 捕获异常')),
              DataCell(Text('无法捕获（void 返回）')),
            ]),
            DataRow(cells: [
              DataCell(Text('外部变化检测')),
              DataCell(Text('不支持')),
              DataCell(Text('支持（listener 通知）')),
            ]),
          ],
        ),
      );
    }
    ```

    **4. 构建平台支持矩阵 (per API-03):**

    ```dart
    Widget _buildPlatformMatrix() {
      Widget _statusIcon(bool supported) {
        return Icon(
          supported ? Icons.check_circle : Icons.cancel,
          color: supported ? Colors.green : Colors.red,
          size: 20,
        );
      }

      return Padding(
        padding: const EdgeInsets.symmetric(horizontal: 16),
        child: DataTable(
          columns: const [
            DataColumn(label: Text('平台')),
            DataColumn(label: Text('fullscreen_window')),
            DataColumn(label: Text('flutter_fullscreen')),
          ],
          rows: [
            DataRow(cells: [
              const DataCell(Text('Windows')),
              DataCell(_statusIcon(true)),
              DataCell(_statusIcon(true)),
            ]),
            DataRow(cells: [
              const DataCell(Text('Linux')),
              DataCell(_statusIcon(true)),
              DataCell(_statusIcon(true)),
            ]),
            DataRow(cells: [
              const DataCell(Text('macOS')),
              DataCell(_statusIcon(false)),  // fullscreen_window 不支持 macOS
              DataCell(_statusIcon(true)),
            ]),
            DataRow(cells: [
              const DataCell(Text('Web')),
              DataCell(_statusIcon(true)),
              DataCell(_statusIcon(true)),
            ]),
            DataRow(cells: [
              const DataCell(Text('Android')),
              DataCell(_statusIcon(true)),
              DataCell(_statusIcon(true)),
            ]),
            DataRow(cells: [
              const DataCell(Text('iOS')),
              DataCell(_statusIcon(true)),
              DataCell(_statusIcon(true)),
            ]),
          ],
        ),
      );
    }
    ```

    **5. 构建架构差异表 (per API-04):**

    ```dart
    Widget _buildArchitectureTable() {
      return Padding(
        padding: const EdgeInsets.symmetric(horizontal: 16),
        child: DataTable(
          columns: const [
            DataColumn(label: Text('维度')),
            DataColumn(label: Text('fullscreen_window')),
            DataColumn(label: Text('flutter_fullscreen')),
          ],
          rows: const [
            DataRow(cells: [
              DataCell(Text('插件类型')),
              DataCell(Text('联邦插件（原生）')),
              DataCell(Text('纯 Dart + 委托')),
            ]),
            DataRow(cells: [
              DataCell(Text('原生代码')),
              DataCell(Text('C++ (Win), C (Linux)')),
              DataCell(Text('无（使用 window_manager）')),
            ]),
            DataRow(cells: [
              DataCell(Text('平台分发')),
              DataCell(Text('MethodChannel')),
              DataCell(Text('条件导入')),
            ]),
            DataRow(cells: [
              DataCell(Text('状态模型')),
              DataCell(Text('无状态（fire-and-forget）')),
              DataCell(Text('有状态（观察者模式）')),
            ]),
            DataRow(cells: [
              DataCell(Text('依赖深度')),
              DataCell(Text('1 层（直接）')),
              DataCell(Text('2 层（→ window_manager）')),
            ]),
          ],
        ),
      );
    }
    ```

    **6. 添加辅助方法：**

    ```dart
    Widget _buildSectionTitle(String title) {
      return Padding(
        padding: const EdgeInsets.fromLTRB(16, 16, 16, 8),
        child: Text(title, style: const TextStyle(fontSize: 14, fontWeight: FontWeight.w600)),
      );
    }
    ```

    **关键实现细节：**
    - ExpansionTile 的 initiallyExpanded: false 默认折叠，减少初始页面长度
    - DataTable 列宽自适应，第三列（flutter_fullscreen）可能需要更宽
    - 平台支持矩阵中 macOS 的 fullscreen_window 显示红色叉（研究确认无 macOS 实现）
    - 所有文本使用中文，与现有界面一致
    - 表格内文字使用较小字号（fontSize: 12）以容纳较长的方法签名
  </action>
  <verify>
    <automated>cd D:/demo_flutter_fullscreen/demo_fullscreen && flutter analyze lib/main.dart 2>&1 | tail -5</automated>
  </verify>
  <done>
    1. API 对比面板使用 ExpansionTile 实现，默认折叠
    2. 方法签名对比表列出两个包的 setFullScreen、isFullScreen、listener、getScreenSize、初始化差异
    3. 返回值差异表对比 Future<void> vs void、状态确认方式、错误处理、外部变化检测
    4. 平台支持矩阵使用绿色勾/红色叉图标，macOS 显示 fullscreen_window 不支持
    5. 架构差异表对比插件类型、原生代码、平台分发、状态模型、依赖深度
    6. flutter analyze 无错误
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 添加依赖信息面板和已知陷阱面板</name>
  <files>demo_fullscreen/lib/main.dart</files>
  <behavior>
    - 页面包含可折叠的"依赖信息"ExpansionTile 面板
    - 依赖信息面板内包含：依赖树对比表、体积差异说明、已知陷阱列表
    - 依赖树使用树形文本展示两个包的直接依赖和传递依赖
    - 体积差异使用文字说明（fullscreen_window 轻量，flutter_fullscreen 包含 window_manager）
    - 已知陷阱列表使用带严重级别标签的列表项，按严重程度排序
    - 陷阱严重级别使用颜色编码：CRITICAL=红色、HIGH=橙色、MEDIUM=黄色、LOW=灰色
    - 陷阱列表包含陷阱编号、标题、严重级别、受影响的包
  </behavior>
  <action>
    **1. 添加依赖信息面板构建方法 (per DEP-01, DEP-02, DEP-03):**

    在 _DualFullscreenPageState 中添加 `_buildDependencyPanel()` 方法：

    ```dart
    Widget _buildDependencyPanel() {
      return Card(
        margin: const EdgeInsets.symmetric(horizontal: 16, vertical: 8),
        child: ExpansionTile(
          title: const Text('依赖信息', style: TextStyle(fontWeight: FontWeight.bold)),
          initiallyExpanded: false,
          children: [
            // 依赖树对比
            _buildSectionTitle('依赖树'),
            _buildDependencyTree(),
            // 体积差异
            _buildSectionTitle('体积差异'),
            _buildSizeComparison(),
            // 已知陷阱
            _buildSectionTitle('已知陷阱'),
            _buildPitfallsList(),
            const SizedBox(height: 16),
          ],
        ),
      );
    }
    ```

    **2. 构建依赖树对比 (per DEP-01):**

    ```dart
    Widget _buildDependencyTree() {
      return Padding(
        padding: const EdgeInsets.symmetric(horizontal: 16),
        child: Row(
          crossAxisAlignment: CrossAxisAlignment.start,
          children: [
            // fullscreen_window 依赖树
            Expanded(
              child: Column(
                crossAxisAlignment: CrossAxisAlignment.start,
                children: [
                  const Text('fullscreen_window', style: TextStyle(fontWeight: FontWeight.w600)),
                  const SizedBox(height: 8),
                  const Text('├── flutter (SDK)'),
                  const Text('├── flutter_web_plugins (SDK)'),
                  const Text('├── plugin_platform_interface ^2.0.2'),
                  const Text('└── web ^1.0.0'),
                  const SizedBox(height: 4),
                  Text('传递依赖: 0', style: TextStyle(color: Colors.grey[600], fontSize: 12)),
                ],
              ),
            ),
            const SizedBox(width: 16),
            // flutter_fullscreen 依赖树
            Expanded(
              child: Column(
                crossAxisAlignment: CrossAxisAlignment.start,
                children: [
                  const Text('flutter_fullscreen', style: TextStyle(fontWeight: FontWeight.w600)),
                  const SizedBox(height: 8),
                  const Text('├── flutter (SDK)'),
                  const Text('├── web ^1.1.0'),
                  const Text('└── window_manager ^0.5.0'),
                  const Text('    ├── screen_retriever'),
                  const Text('    ├── gtk (Linux)'),
                  const Text('    └── 原生代码 (Win/Mac/Linux)'),
                  const SizedBox(height: 4),
                  Text('传递依赖: 3+', style: TextStyle(color: Colors.orange[700], fontSize: 12)),
                ],
              ),
            ),
          ],
        ),
      );
    }
    ```

    **3. 构建体积差异说明 (per DEP-02):**

    ```dart
    Widget _buildSizeComparison() {
      return Padding(
        padding: const EdgeInsets.symmetric(horizontal: 16),
        child: Column(
          crossAxisAlignment: CrossAxisAlignment.start,
          children: [
            _buildSizeRow('fullscreen_window', '轻量', '仅 platform_interface + web 互操作', Colors.green),
            const SizedBox(height: 8),
            _buildSizeRow('flutter_fullscreen', '较重', '包含 window_manager 及其传递依赖（screen_retriever、GTK 绑定、原生代码）', Colors.orange),
            const SizedBox(height: 8),
            Text(
              '注意: flutter_fullscreen 的 window_manager 依赖会增加构建时间和二进制体积，但对演示项目可接受。',
              style: TextStyle(color: Colors.grey[600], fontSize: 12, fontStyle: FontStyle.italic),
            ),
          ],
        ),
      );
    }

    Widget _buildSizeRow(String package, String label, String description, Color color) {
      return Row(
        crossAxisAlignment: CrossAxisAlignment.start,
        children: [
          Container(
            padding: const EdgeInsets.symmetric(horizontal: 8, vertical: 2),
            decoration: BoxDecoration(
              color: color.withOpacity(0.1),
              borderRadius: BorderRadius.circular(4),
              border: Border.all(color: color.withOpacity(0.3)),
            ),
            child: Text(label, style: TextStyle(color: color, fontSize: 12, fontWeight: FontWeight.w600)),
          ),
          const SizedBox(width: 8),
          Expanded(
            child: RichText(
              text: TextSpan(
                style: const TextStyle(color: Colors.black87, fontSize: 13),
                children: [
                  TextSpan(text: '$package: ', style: const TextStyle(fontWeight: FontWeight.w600)),
                  TextSpan(text: description),
                ],
              ),
            ),
          ),
        ],
      );
    }
    ```

    **4. 构建已知陷阱列表 (per DEP-03):**

    ```dart
    Widget _buildPitfallsList() {
      // 陷阱数据，按严重程度排序
      final pitfalls = [
        {'id': 1, 'title': '状态不同步', 'severity': 'CRITICAL', 'package': '两者'},
        {'id': 2, 'title': '缺少初始化', 'severity': 'HIGH', 'package': 'flutter_fullscreen'},
        {'id': 3, 'title': 'Windows 布局异常', 'severity': 'HIGH', 'package': 'fullscreen_window'},
        {'id': 4, 'title': '全局可变状态', 'severity': 'MEDIUM', 'package': 'fullscreen_window'},
        {'id': 5, 'title': 'WS_EX_TOPMOST 残留', 'severity': 'MEDIUM', 'package': 'fullscreen_window'},
        {'id': 6, 'title': '传递依赖复杂', 'severity': 'MEDIUM', 'package': 'flutter_fullscreen'},
        {'id': 7, 'title': 'Async vs Void 混淆', 'severity': 'MEDIUM', 'package': '两者'},
        {'id': 8, 'title': 'macOS 动画行为', 'severity': 'MEDIUM', 'package': 'fullscreen_window'},
        {'id': 9, 'title': '无平台能力检测', 'severity': 'MEDIUM', 'package': '两者'},
        {'id': 10, 'title': '监听器内存泄漏', 'severity': 'MEDIUM', 'package': 'flutter_fullscreen'},
        {'id': 11, 'title': '字符串异常', 'severity': 'LOW', 'package': 'flutter_fullscreen'},
        {'id': 12, 'title': 'Web 浏览器限制', 'severity': 'MEDIUM', 'package': '两者'},
        {'id': 13, 'title': 'getScreenSize 差异', 'severity': 'LOW', 'package': 'fullscreen_window'},
        {'id': 14, 'title': '快速切换竞态', 'severity': 'MEDIUM', 'package': '两者'},
        {'id': 15, 'title': '命名约定问题', 'severity': 'LOW', 'package': 'fullscreen_window'},
        {'id': 16, 'title': 'window_manager 副作用', 'severity': 'MEDIUM', 'package': 'flutter_fullscreen'},
      ];

      return Padding(
        padding: const EdgeInsets.symmetric(horizontal: 16),
        child: Column(
          children: pitfalls.map((p) => _buildPitfallItem(p)).toList(),
        ),
      );
    }

    Widget _buildPitfallItem(Map<String, dynamic> pitfall) {
      Color severityColor;
      switch (pitfall['severity']) {
        case 'CRITICAL':
          severityColor = Colors.red;
          break;
        case 'HIGH':
          severityColor = Colors.orange;
          break;
        case 'MEDIUM':
          severityColor = Colors.amber[700]!;
          break;
        case 'LOW':
          severityColor = Colors.grey;
          break;
        default:
          severityColor = Colors.grey;
      }

      return Padding(
        padding: const EdgeInsets.symmetric(vertical: 4),
        child: Row(
          crossAxisAlignment: CrossAxisAlignment.start,
          children: [
            // 陷阱编号
            SizedBox(
              width: 32,
              child: Text('#${pitfall['id']}', style: const TextStyle(fontWeight: FontWeight.w600, fontSize: 12)),
            ),
            // 严重级别标签
            Container(
              width: 72,
              padding: const EdgeInsets.symmetric(horizontal: 6, vertical: 2),
              decoration: BoxDecoration(
                color: severityColor.withOpacity(0.1),
                borderRadius: BorderRadius.circular(4),
                border: Border.all(color: severityColor.withOpacity(0.3)),
              ),
              child: Text(
                pitfall['severity'],
                style: TextStyle(color: severityColor, fontSize: 10, fontWeight: FontWeight.w600),
                textAlign: TextAlign.center,
              ),
            ),
            const SizedBox(width: 8),
            // 标题和受影响包
            Expanded(
              child: RichText(
                text: TextSpan(
                  style: const TextStyle(color: Colors.black87, fontSize: 13),
                  children: [
                    TextSpan(text: '${pitfall['title']} ', style: const TextStyle(fontWeight: FontWeight.w500)),
                    TextSpan(
                      text: '(${pitfall['package']})',
                      style: TextStyle(color: Colors.grey[600], fontSize: 12),
                    ),
                  ],
                ),
              ),
            ),
          ],
        ),
      );
    }
    ```

    **5. 在 ListView 中添加依赖面板：**

    在现有的 ListView children 中，在日志面板之前添加依赖信息面板：
    ```dart
    // 现有的包控制区 Row
    SizedBox(height: 400, child: Row(...)),
    // API 对比面板（来自 Task 1）
    _buildApiComparisonPanel(),
    // 依赖信息面板（新增）
    _buildDependencyPanel(),
    // 日志面板
    _buildLogPanel(),
    ```

    **关键实现细节：**
    - 依赖树使用树形字符（├── └──）展示层级关系
    - 体积差异使用颜色标签（绿色=轻量，橙色=较重）直观对比
    - 陷阱列表按严重程度排序（CRITICAL > HIGH > MEDIUM > LOW）
    - 每个陷阱显示编号、严重级别标签、标题、受影响的包
    - 严重级别标签使用颜色编码：红色(CRITICAL)、橙色(HIGH)、黄色(MEDIUM)、灰色(LOW)
    - 陷阱数据来自 PITFALLS.md 的 Pitfall Matrix，共 16 个陷阱
  </action>
  <verify>
    <automated>cd D:/demo_flutter_fullscreen/demo_fullscreen && flutter analyze lib/main.dart 2>&1 | tail -5</automated>
  </verify>
  <done>
    1. 依赖信息面板使用 ExpansionTile 实现，默认折叠
    2. 依赖树对比展示两个包的直接依赖和传递依赖
    3. 体积差异说明使用颜色标签直观对比
    4. 已知陷阱列表按严重程度排序，使用颜色编码标签
    5. 陷阱列表包含 16 个陷阱，显示编号、严重级别、标题、受影响包
    6. flutter analyze 无错误
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI -> Static Data | 所有对比数据来自研究文档，硬编码在 UI 中，无外部信任边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03 | Information Disclosure | 硬编码的依赖和陷阱信息 | low | accept | 演示项目，信息来自公开研究文档，无敏感数据 |
</threat_model>

<verification>
1. `flutter analyze lib/main.dart` 无错误无警告
2. 运行应用后：
   - 点击"API 对比"展开 -> 显示方法签名、返回值、平台支持、架构差异四个表格
   - 平台支持矩阵中 macOS 行的 fullscreen_window 列显示红色叉
   - 点击"依赖信息"展开 -> 显示依赖树、体积差异、已知陷阱
   - 陷阱列表按严重程度排序，CRITICAL 显示红色标签
   - 所有面板可独立折叠/展开
3. 页面垂直滚动，包含：包控制区、API 对比、依赖信息、调用日志
</verification>

<success_criteria>
1. API 对比面板展示方法签名、返回值、平台支持、架构差异四个维度的对比数据
2. 依赖信息面板展示依赖树、体积差异、已知陷阱三个维度的信息
3. 平台支持矩阵正确显示 macOS 的 fullscreen_window 不支持（红色叉）
4. 已知陷阱列表包含 16 个陷阱，按严重程度排序，使用颜色编码
5. 所有面板默认折叠，可独立展开
6. flutter analyze 零错误零警告
</success_criteria>

<output>
Create `.planning/phases/02-enhancement/02-02-SUMMARY.md` when done
</output>
